#include "main.h"

/**
 * _abs - prints lowercase and new line
 *
 * @n: takes an integer value or a character value
 *
 * Return: return 0 when is not lower case return 1 when lower case
 *
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
